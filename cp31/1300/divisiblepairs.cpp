#include<bits/stdc++.h>
using namespace std;

void solve() {
    int n, x, y; cin >> n >> x >> y;
    vector<int> arr(n);
    for (int i=0; i<n; i++) cin >> arr[i];
    map<pair<int, int>, int> M;
    int ans = 0;
    for (int i=0; i<n; i++) {
        
    }
    cout << ans << "\n";
}

int32_t main() {
    ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int t; cin >> t;
    while(t--) solve();
}